#include "lists.h"
/**
 *
 *
 *
 *
 */

int sum_listint(listint_t *head)
{
	int sum;
	listint_t *buff;

	buff = head;
	sum = 0;
	while (buff != NULL)
	{
		sum += buff->n;
		buff = buff->next;
	}
	return (sum);
}
